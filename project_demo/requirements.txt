# Demo-only dependencies. The root requirements.txt / requirements-lock.txt are the audited
# record of the stack every reported number was produced with, and are deliberately untouched.
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9   # /api/upload receives multipart form data
