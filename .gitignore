.venv/
__pycache__/
*.pyc
/data/
logs/
results/**/*.pt
results/**/features/
.pytest_cache/
.DS_Store

# linear heads are ~16 KB each — keep the shipped one (and the previous candidate) so the repo is self-contained
!results/frozen_siglip2_giant_ship/head_best.pt
!results/frozen_siglip2_giant_mjv5/head_best.pt

# demo artefacts (scripts/demo.sh)
demo_images/
demo_preds.json

# project_demo media (project_demo/fetch_videos.py) — fetched from the original hosts at run
# time and never redistributed. See docs/DATA_LICENSES.md.
project_demo/videos/
project_demo/videos_raw/
project_demo/uploads/
